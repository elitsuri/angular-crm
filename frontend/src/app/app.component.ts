import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <div class="app-container">
      <h1>Angular CRM</h1>
      <p>Angular CRM application with contacts, deals pipeline, and email integration</p>
    </div>
  `,
  styles: [``]
})
export class AppComponent {}
